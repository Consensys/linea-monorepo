# =============================================================================
# Verifier UI Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Storage Mode
# -----------------------------------------------------------------------------
# Controls how files and sessions are stored.
#
# Options:
#   - "client" (default): Uses browser IndexedDB for storage. No server required.
#                         Verification runs directly in the browser.
#   - "server": Uses API routes for storage and verification. (Not implemented)
#
# For most use cases, leave this as "client" for a fully client-side experience.
NEXT_PUBLIC_STORAGE_MODE=client

# -----------------------------------------------------------------------------
# Static Export (Build-time only)
# -----------------------------------------------------------------------------
# Set to "true" to build a fully static export.
# This creates HTML/JS/CSS files that can be hosted on any static file server
# (GitHub Pages, Vercel Static, AWS S3, etc.)
#
# Usage:
#   STATIC_EXPORT=true pnpm build
#
# Note: Static export mode requires NEXT_PUBLIC_STORAGE_MODE=client
# STATIC_EXPORT=true

# -----------------------------------------------------------------------------
# Server Mode Configuration (only used when NEXT_PUBLIC_STORAGE_MODE=server)
# -----------------------------------------------------------------------------
# These settings are only relevant if server mode is implemented in the future.

# Directory for storing uploaded files (defaults to ~/.linea-verifier-ui)
# VERIFIER_UPLOADS_DIR=/path/to/uploads

# Session expiry in hours (defaults to 24)
# SESSION_EXPIRY_HOURS=24

# Maximum file size in bytes (defaults to 10MB)
# MAX_FILE_SIZE=10485760

# Maximum total session size in bytes (defaults to 50MB)
# MAX_SESSION_SIZE=52428800
