###############################################################################
#                         NETWORK & AUTHENTICATION                          #
# See hardhat.config.ts for how these map to Hardhat network names.         #
###############################################################################

# RPC endpoints
L1_RPC_URL="https://sepolia.infura.io/v3/<infura_key>"
L2_RPC_URL="https://linea-sepolia.infura.io/v3/<infura_key>"
# CUSTOM_RPC_URL=
# INFURA_API_KEY=

# Private keys — set the one matching your target network
PRIVATE_KEY=
# L2_PRIVATE_KEY=
# SEPOLIA_PRIVATE_KEY=
# LINEA_SEPOLIA_PRIVATE_KEY=
# MAINNET_PRIVATE_KEY=
# LINEA_MAINNET_PRIVATE_KEY=
# CUSTOM_PRIVATE_KEY=

# Block explorer verification
ETHERSCAN_API_KEY=
# VERIFY_CONTRACT=true

###############################################################################
#                          SHARED ACROSS CHAINS                             #
# Variables consumed by contracts on both L1 and L2.                        #
###############################################################################

# Security councils — one per layer, reused by all contracts on that layer.
# L1: LineaRollup, Validium, TokenBridge (L1), RecoverFunds,
#     YieldManager, CallForwardingProxy
# L2: L2MessageService, TokenBridge (L2), RollupRevenueVault
L1_SECURITY_COUNCIL="0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266"
L2_SECURITY_COUNCIL="0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266"

# Safe multisig addresses (for operational multisig transactions)
L1_SAFE_ADDRESS=
L2_SAFE_ADDRESS=

# Timelock — can be deployed on either chain
TIMELOCK_PROPOSERS="0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266"
TIMELOCK_EXECUTORS="0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266"
# TIMELOCK_ADMIN_ADDRESS=
# MIN_DELAY=0

###############################################################################
#                   DUAL-CHAIN DEPLOYMENTS (L1 or L2)                       #
# TokenBridge and BridgedToken are deployed once per chain.                 #
# Set TOKEN_BRIDGE_L1=true for L1 deploy, omit/false for L2.               #
###############################################################################

# TOKEN_BRIDGE_L1=
# TOKEN_BRIDGE_ADDRESS=
L1_RESERVED_TOKEN_ADDRESSES=
L2_RESERVED_TOKEN_ADDRESSES=
# REMOTE_CHAIN_ID=
# REMOTE_SENDER_ADDRESS=
# BRIDGED_TOKEN_ADDRESS=

# Custom bridged token (either chain)
# CUSTOMTOKENBRIDGE_NAME=
# CUSTOMTOKENBRIDGE_SYMBOL=
# CUSTOMTOKENBRIDGE_DECIMALS=
# CUSTOMTOKENBRIDGE_BRIDGE_ADDRESS=

###############################################################################
#                            L1 CONTRACTS                                   #
###############################################################################

# ── Shared L1 base (LineaRollup & Validium) ────────────────────────────────
# Common initialization params — both products share the same contract base.
L1_INITIAL_L2_BLOCK_NUMBER="0"
L1_INITIAL_STATE_ROOT_HASH="0x113e9977cebf08f3b271d121342540fce95530c206c2a878ae957925e1d0fc02"
L1_GENESIS_TIMESTAMP=

# ── Verifier (PlonkVerifier) ───────────────────────────────────────────────
# Proof verifier deployed on L1, called by LineaRollup/Validium.
VERIFIER_CONTRACT_NAME="PlonkVerifier"
VERIFIER_PROOF_TYPE="1"
VERIFIER_ADDRESS="0x5FbDB2315678afecb367f032d93F642f64180aa3"
# VERIFIER_CHAIN_ID=
# VERIFIER_BASE_FEE=
# VERIFIER_COINBASE=
# VERIFIER_L2_MESSAGE_SERVICE_ADDRESS=

# ── LineaRollup ────────────────────────────────────────────────────────────
LINEA_ROLLUP_ADDRESS="0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9"
LINEA_ROLLUP_OPERATORS="0x70997970C51812dc3A010C7d01b50e0d17dc79C8"
LINEA_ROLLUP_RATE_LIMIT_PERIOD="86400" #24Hours in seconds
LINEA_ROLLUP_RATE_LIMIT_AMOUNT="1000000000000000000000" #1000ETH

# ── Validium ───────────────────────────────────────────────────────────────
# Product-specific overrides; shared base params (L1_INITIAL_*, L1_GENESIS_*)
# are above.
# VALIDIUM_OPERATORS=
# VALIDIUM_RATE_LIMIT_PERIOD="86400"
# VALIDIUM_RATE_LIMIT_AMOUNT="1000000000000000000000"

# ── Yield Manager & Native Yield (L1) ─────────────────────────────────────
NATIVE_YIELD_AUTOMATION_SERVICE_ADDRESS=
# YIELD_MANAGER_ADDRESS=
VAULT_HUB=
VAULT_FACTORY=
STETH=
MINIMUM_WITHDRAWAL_RESERVE_PERCENTAGE_BPS=
TARGET_WITHDRAWAL_RESERVE_PERCENTAGE_BPS=
MINIMUM_WITHDRAWAL_RESERVE_AMOUNT=
TARGET_WITHDRAWAL_RESERVE_AMOUNT=

# ── L1 Linea Token Burner ─────────────────────────────────────────────────
# Burns LINEA tokens on L1 after cross-chain claims.
# LINEA_TOKEN_BURNER_MESSAGE_SERVICE=
# LINEA_TOKEN_BURNER_LINEA_TOKEN=

# ── RecoverFunds (L1) ─────────────────────────────────────────────────────
# RECOVERFUNDS_EXECUTOR_ADDRESS=

# ── CallForwardingProxy (L1) ──────────────────────────────────────────────
# Forwards calls to LINEA_ROLLUP_ADDRESS (set above).

###############################################################################
#                            L2 CONTRACTS                                   #
###############################################################################

# ── L2 Message Service ────────────────────────────────────────────────────
L2_MESSAGE_SERVICE_L1L2_MESSAGE_SETTER="0x90F79bf6EB2c4f870365E785982E1f101E93b906"
L2_MESSAGE_SERVICE_RATE_LIMIT_PERIOD="86400" #24Hours in seconds
L2_MESSAGE_SERVICE_RATE_LIMIT_AMOUNT="1000000000000000000000" #1000ETH
# L2_MESSAGE_SERVICE_ADDRESS=

# ── EIP System Contracts (L2 predeploys) ──────────────────────────────────
# EIP-2935 historical block hashes and EIP-4788 beacon block root.
# Deployed to fixed addresses; no env vars needed beyond private key.

# ── Upgradeable Predeploys (L2) ──────────────────────────────────────────
# EIP-7002 WithdrawalQueue, EIP-7251 ConsolidationQueue, EIP-6110 BeaconChainDeposit.
# Placeholder implementations; no contract-specific env vars.

# ── V3 DEX Swap Adapter (L2) ─────────────────────────────────────────────
# V3_DEX_SWAP_ADAPTER_ROUTER=
# V3_DEX_SWAP_ADAPTER_WETH_TOKEN=
# V3_DEX_SWAP_ADAPTER_LINEA_TOKEN=
# V3_DEX_SWAP_ADAPTER_POOL_TICK_SPACING=

# ── Rollup Revenue Vault (L2) ─────────────────────────────────────────────
# Receives rollup revenue and handles LINEA token burns.
# ROLLUP_REVENUE_VAULT_ADDRESS=
# ROLLUP_REVENUE_VAULT_LAST_INVOICE_DATE=
# ROLLUP_REVENUE_VAULT_INVOICE_SUBMITTER=
# ROLLUP_REVENUE_VAULT_BURNER=
# ROLLUP_REVENUE_VAULT_INVOICE_PAYMENT_RECEIVER=
# ROLLUP_REVENUE_VAULT_TOKEN_BRIDGE=
# ROLLUP_REVENUE_VAULT_MESSAGE_SERVICE=
# ROLLUP_REVENUE_VAULT_L1_LINEA_TOKEN_BURNER=
# ROLLUP_REVENUE_VAULT_LINEA_TOKEN=
# ROLLUP_REVENUE_VAULT_DEX_SWAP_ADAPTER=

# ── Sequencer Uptime Feed (L2, Chainlink-compatible) ─────────────────────
# LINEA_SEQUENCER_UPTIME_FEED_INITIAL_STATUS=
# LINEA_SEQUENCER_UPTIME_FEED_ADMIN=
# LINEA_SEQUENCER_UPTIME_FEED_UPDATER=

###############################################################################
#                      OPERATIONAL & UPGRADE SCRIPTS                        #
###############################################################################

# Operational tasks (setRateLimit, setVerifierAddress, etc.)
WITHDRAW_LIMIT_IN_WEI=100000000000000000000000
MESSAGE_SERVICE_ADDRESS="0xe537D669CA013d86EBeF1D64e40fC74CADC91987"
MESSAGE_SERVICE_TYPE="L2MessageService"

# Upgrade scripts (ImplementationForProxy, *WithReinitialization)
NEW_CONTRACT_NAME="LineaRollup"
# OLD_CONTRACT_NAME=
PROXY_ADDRESS="0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9"
# CONTRACT_NAME=

# ── Payment Splitter (either chain) ──────────────────────────────────────
# PAYMENT_SPLITTER_PAYEES=
# PAYMENT_SPLITTER_SHARES=

###############################################################################
#                           TESTING ONLY                                    #
###############################################################################

# TEST_ERC20_NAME=
# TEST_ERC20_SYMBOL=
# TEST_ERC20_INITIAL_SUPPLY=
