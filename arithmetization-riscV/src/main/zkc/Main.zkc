pub input risc_program(address:u20)->(len:u32)
pub input pi(address:u20)->(bytes:u8)
memory ram(address:u32)->(bytes:u8)

// TODO @Ghost
fn main() {

// TODO: memory pre loading (r5 + pi)

var pc:u32 // TODO: initialize me
var opcode:u32
var timestamp:u64

// TODO: while loop on the r5 opcodes through the interpreter, until final opcode
opcode = read(ram, pc, 4)
pc = interpreter(opcode)

}


