include "../lib/div_32.zkc"

pub input data(address:u2) -> (data:u32)
pub output result(address:u1) -> (result:u32)

fn main() {
    if data[2] == 0 {
        result[0] = divu(data[0], data[1])
        return
    }
    result[0] = remu(data[0], data[1])
    return
}
