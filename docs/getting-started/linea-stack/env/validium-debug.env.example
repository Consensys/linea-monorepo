# Optional Validium debugging knob.
#
# Copy into ../.env only if you are intentionally debugging the unvalidated
# Validium path. The validated quickstart path is ROLLUP.

LINEA_COORDINATOR_DATA_AVAILABILITY=VALIDIUM
