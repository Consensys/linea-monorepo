# Optional partial-prover validation mode.
#
# Copy these values into ../.env only when Docker Desktop has enough memory.
# Use `docker compose ... down -v --remove-orphans` before switching modes.

PROVER_DEV_OVERRIDE=false
PROVER_GOMEMLIMIT=24GiB
