package inclusion

import "github.com/consensys/linea-monorepo/prover/protocol/wizard"

// It scans the initial compiledIOP and group all the checkedTables related to the same lookupTable,
// It creates a frequency column M for all such pairs (checkedTables, lookupTable).
func prepareLookup(initalComp *wizard.CompiledIOP) {
	panic("unimplemented")
}
