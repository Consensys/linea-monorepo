version: "2"
run:
  issues-exit-code: 1
linters:
  default: none
  enable:
    - gosec
    - ineffassign
    - prealloc
  settings:
    gosec:
      excludes:
        - G115
    staticcheck:
      checks:
        - all
        - -SA1006
        - -SA1019
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - path: (.+)\.go$
        text: '.*printf: non-constant format string in call to.*'
    paths:
      - compressor
      - zkevm/arithmetization
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - compressor
      - zkevm/arithmetization
      - third_party$
      - builtin$
      - examples$
