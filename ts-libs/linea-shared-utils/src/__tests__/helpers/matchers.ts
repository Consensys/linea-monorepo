/**
 * Custom Jest matchers and assertion helpers for unit tests.
 *
 * Add custom matchers here if needed for specialized assertions
 * that improve test readability and reduce duplication.
 */

// Placeholder for future custom matchers
// Example:
// expect.extend({
//   toBeValidAddress(received: string) {
//     const pass = /^0x[a-fA-F0-9]{40}$/.test(received);
//     return {
//       pass,
//       message: () => `expected ${received} to be a valid Ethereum address`,
//     };
//   },
// });

export {};
