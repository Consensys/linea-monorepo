export interface IContractClientLibrary {
  
  // retryTransactionWithHigherFee(
  //   transactionHash: string,
  //   priceBumpPercent: number,
  // ): Promise<void>

  //   parseTransactionError(transactionHash: string): Promise<string>
}
