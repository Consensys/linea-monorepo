name: Maven Release all JVM-Libs

permissions:
  contents: write
  actions: read
  packages: write

on:
  workflow_dispatch:
    inputs:
      version:
        required: false
        type: string
        description: 'Release version: e.g "1.0.0". Defaults to 0.0.1-v<YYYYmmDDHHMMSS> if not provided'

jobs:
  release:
    runs-on: gha-runner-scale-set-ubuntu-22.04-amd64-med
    steps:
      - name: Set version
        id: name
        run: |
          TIMESTAMP=`date -u +'%Y%m%d%H%M%S'`
          INPUT_VERSION=${{ github.event.inputs.version }}
          if [ -z "$INPUT_VERSION" ]; then
            VERSION="0.0.1-v$TIMESTAMP"
          else
            VERSION=$INPUT_VERSION
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Print version
        run: |
          echo "VERSION: $VERSION"

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 #v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@8379f6a1328ee0e06e2bb424dadb7b159856a326 # 4.4.0

      - name: Clean
        # ./gradlew clean is necessary because the build is cached
        # and cause issues with JReleaser
        run: ./gradlew clean

      - name: Build
        run: ./gradlew -p jvm-libs build
        env:
          GITHUB_TOKEN: ${{ secrets._GITHUB_TOKEN_RELEASE_ACCESS }}

      - name: Deploy to Local File System
        run: ./gradlew -p jvm-libs publish -Pversion=$VERSION
        env:
          GITHUB_TOKEN: ${{ secrets._GITHUB_TOKEN_RELEASE_ACCESS }}

      - name: Set release metadata
        run: |
          echo "JRELEASER_TAG_NAME=maven-jvm-libs-v${VERSION}" >> $GITHUB_ENV
          echo "JRELEASER_RELEASE_NAME=Maven release: all JVM libs v${VERSION}" >> $GITHUB_ENV

      - name: Release to Maven Central
        run: ./gradlew -p jvm-libs jreleaserRelease
        env:
          JRELEASER_PROJECT_VERSION: ${{ env.VERSION }}
          JRELEASER_TAG_NAME: ${{ env.JRELEASER_TAG_NAME }}
          JRELEASER_RELEASE_NAME: ${{ env.JRELEASER_RELEASE_NAME }}
          JRELEASER_GITHUB_TOKEN: ${{ secrets._GITHUB_TOKEN_RELEASE_ACCESS }}
          JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
          JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
          JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
          JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.JRELEASER_MAVENCENTRAL_USERNAME }}
          JRELEASER_MAVENCENTRAL_PASSWORD: ${{ secrets.JRELEASER_MAVENCENTRAL_PASSWORD }}

      # Persist logs
      - name: JReleaser release output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jreleaser-release
          path: |
            out/jreleaser/trace.log
            out/jreleaser/output.properties
