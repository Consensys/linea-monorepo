name: Test Actions Enabled

on:
  push:
    branches: [ develop ]

jobs:
  test-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Test if Actions work
        run: |
          echo "✅ GitHub Actions are enabled and working"
          echo "✅ Standard runners are available"