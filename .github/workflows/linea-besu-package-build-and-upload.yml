name: linea-besu-package-build-and-upload

permissions:
  contents: read
  actions: read
  packages: read

on:
  workflow_call:
    inputs:
      release_tag_prefix:
        required: true
        type: string
      with_besu_fleet_plugin:
        required: true
        type: boolean
    outputs:
      linea_besu_package_tag:
        value: ${{ jobs.build-and-upload-artifact.outputs.linea_besu_package_tag }}
      linea_besu_package_release_tag:
        value: ${{ jobs.build-and-upload-artifact.outputs.linea_besu_package_release_tag }}
      dockerimage:
        value: ${{ jobs.build-and-upload-artifact.outputs.dockerimage }}
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false
      FLEET_GITHUB_APP_ID:
        required: false
      FLEET_GITHUB_APP_PRIVATE_KEY:
        required: false

jobs:
  build-and-upload-artifact:
    runs-on: gha-runner-scale-set-ubuntu-22.04-amd64-med
    outputs:
      linea_besu_package_tag: ${{ steps.build-plugins-and-assemble.outputs.dockertag }}
      linea_besu_package_release_tag: ${{ steps.build-plugins-and-assemble.outputs.releasetag }}
      dockerimage: ${{ steps.build-plugins-and-assemble.outputs.dockerimage }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build plugins and assemble
        id: build-plugins-and-assemble
        uses: ./.github/actions/linea-besu-package/build-plugins-and-assemble
        with:
          release_tag_prefix: ${{ inputs.release_tag_prefix }}
          with_besu_fleet_plugin: ${{ inputs.with_besu_fleet_plugin }}
          fleet_github_app_id: ${{ secrets.FLEET_GITHUB_APP_ID }}
          fleet_github_app_private_key: ${{ secrets.FLEET_GITHUB_APP_PRIVATE_KEY }}

      - name: set up docker buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 #v3.11.1

      - name: set docker build args
        run: |
          echo "Building docker tag: ${{ steps.build-plugins-and-assemble.outputs.dockertag }}"
          echo "Building docker image: ${{ steps.build-plugins-and-assemble.outputs.dockerimage }}"

      - name: build the combined manifest
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 #v6.18.0
        env:
          DOCKER_BUILD_SUMMARY: false
        with:
          context: linea-besu-package/linea-besu/.
          platforms: linux/amd64
          provenance: false
          build-args: |
            VERSION=${{ steps.build-plugins-and-assemble.outputs.dockertag }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ steps.build-plugins-and-assemble.outputs.build_date }}
          load: true
          push: false
          tags: |
            consensys/linea-besu-package:${{ steps.build-plugins-and-assemble.outputs.dockertag }}
            consensys/linea-besu-package-with-fleet:${{ steps.build-plugins-and-assemble.outputs.dockertag }}

      - name: Save Docker image as artifact
        run: |
          docker images
          docker save consensys/linea-besu-package:${{ steps.build-plugins-and-assemble.outputs.dockertag }} consensys/linea-besu-package-with-fleet:${{ steps.build-plugins-and-assemble.outputs.dockertag }} | gzip > linea-besu-package-images.tar.gz
        shell: bash

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: linea-besu-package
          path: linea-besu-package-images.tar.gz
          retention-days: 1
